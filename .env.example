# HuggingFace Hub token (optional, only needed for gated models or higher rate limits)
HF_TOKEN=your_huggingface_token_here

# MLflow tracking URI (defaults to local file store if not set)
MLFLOW_TRACKING_URI=file:///./mlflow/mlruns